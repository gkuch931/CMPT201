#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
  char *line = NULL;
  size_t len = 0;
  getline(&line, &len, stdin);
  if (fork() == 0) {
    printf("command to be run: %s", line);
    // execl(line, line, NULL);
  }
}
