#include <stdint.h>
#include <stdio.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header *next;
};

int main() {
  intptr_t *program_break = sbrk(256);
  //*program_break=
  printf("%lu\n", sizeof(intptr_t));
  printf("%lu\n", sizeof(uint64_t));
}
