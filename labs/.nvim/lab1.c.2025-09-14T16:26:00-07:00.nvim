#define _GNU_SOURCE
#include <stdio.h>
#include <string.h>
int main() {
  char *line = NULL;
  size_t len = 0;
  char *saveptr;
  getline(&line, &len, stdin);
  strtok_r(line, " ", saveptr);
  printf("%s", line);
}
